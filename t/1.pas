program a;
VAR
    f : integer;
    g : real;
    h : boolean;
    j : integer;

function test(a:integer;b:string):char;
var s1:real;
begin
    f:=b;
    g:=c;
    if g<=c then
    h:=ture
    else
    h:=false;
end;
